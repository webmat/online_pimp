module OnlinePimp::Verificators
  <%# service_name.constantize would remove the s unnecessarily %>
  class <%= service_name.capitalize %> < Base
    def initialize(name)
      @name = "http://<%= service_name.downcase %>.com/#{name}"
    end
  end
end
